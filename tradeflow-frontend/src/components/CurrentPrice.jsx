// function CurrentPrice({ currentSelectedStock }) {
//   if (currentSelectedStock === "APPL") {
//     return <p>AAPL: $150.25</p>;
//   } else {
//     return <p>Select a stock to see price</p>;
//   }
// }

function CurrentPrice({ currentSelectedStock }) {
  // console.log("i am here!!!");
  // let timeSeries = currentSelectedStock.data["Time Series (5min)"];
  // let latestTimestamp = null;
  // if (timeSeries) {
  //   latestTimestamp = Object.keys(timeSeries)[0];
  // } else {
  //   console.log("Object is null or undefined.");
  // }
  // let latestData = timeSeries[latestTimestamp];
  // let CurrentPrice = latestData["4. close"];
  // return CurrentPrice;
}

export default CurrentPrice;
